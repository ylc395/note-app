import type PdfViewer from 'web/infra/PdfViewer';

// eslint-disable-next-line mobx/missing-observer
export default (function MarkTooltip({ pdfViewer }: { pdfViewer: PdfViewer | null }) {
  return <button>高亮</button>;
});
