{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["DOM", "ESNext", "DOM.Iterable"],
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": {
      "shared/*": ["../shared/*"],
      "web/*": ["../client/driver/web/*"]
    }
  }
}
