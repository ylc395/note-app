{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "module": "NodeNext",
    "types": ["node"],
    "baseUrl": ".",
    "composite": true,
    "rootDir": "../"
  }
}
